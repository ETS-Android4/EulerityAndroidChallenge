package com.example.eulerityandroidchallenge.photoediting.tools;

/**
 *      An enum specifying the possible types of tools in the editor
 */

public enum ToolType {
    SHAPE,
    TEXT,
    ERASER,
    FILTER,
    EMOJI,
    STICKER
}
